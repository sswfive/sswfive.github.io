---
title: python批量修改文件夹名称
date: 2020-12-23 07:58:21
permalink: /pages/48905d/
categories:
  - Python
tags:
  - python案例
simplePostListLength: 20
---


## 需求：把code文件夹所有的文件重命名为wss_code

```python
# coding=utf-8
import os

flag = 2
file_list = os.listdir()  # 获取code文件夹的目录列表
print(file_list)

for i in file_list:
    if flag == 1:
        # new_name = 'wss_' + 原文件i
        new_name = 'wss_' + i
    elif flag == 2:
        num = len('wss_')  # 删除前缀
        new_name = i[num:]
    os.rename(i, new_name)  # 重命名
```